// This component is superseded by VideoResult.tsx in the new design.
// It is kept here to avoid breaking any legacy imports but is no longer rendered.
export default function VideoCard() {
  return null;
}

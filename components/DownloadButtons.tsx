// This component is superseded by VideoResult.tsx in the new design.
// Kept here to avoid breaking any legacy imports but is no longer rendered.
export default function DownloadButtons() {
  return null;
}
